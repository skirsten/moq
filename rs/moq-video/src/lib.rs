//! Native video encoding/decoding for Media over QUIC.
//!
//! Counterpart to [`moq-audio`](https://crates.io/crates/moq-audio) for
//! video tracks. Currently a placeholder; the codec API will follow the
//! same `Encoder` / `Decoder` / `Producer` / `Consumer` shape once the
//! input/output story for video samples is settled.
